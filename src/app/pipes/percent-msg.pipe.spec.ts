import { PercentMsgPipe } from './percent-msg.pipe';

describe('PercentMsgPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentMsgPipe();
    expect(pipe).toBeTruthy();
  });
});
